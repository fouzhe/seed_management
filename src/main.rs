extern crate seed_management;
use seed_management::SeedManagement;


fn main() {
    let seed_m = SeedManagement::new("124");
    // while true {
    //     seed = seed_management.get_a_seed();
    //     cov = run_target(seed);
    //     seed_management.deal_cov(seed, cov);

    // }

}

